// Discord Application ID는 공개 식별자입니다. 실제 연동 시 아래 문자열만 교체하세요.
// apiBaseUrl: 빈 문자열이면 같은 도메인(프록시)을 통해 백엔드에 연결합니다.
// 배포 환경에서는 server.js의 프록시가 BACKEND_URL 환경변수를 사용합니다.
// 이 값은 server.js 시작 시 BACKEND_URL 환경변수로 자동 교체됩니다.
window.PRAIN_CONFIG = {
  discordClientId: '',
  apiBaseUrl: '',
  demoModeOnApiError: true,
};
